version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: lead-database
    restart: always
    ports:
      - "27018:27017"
    volumes:
      - mongo_data:/data/db 

volumes:
  mongo_data: